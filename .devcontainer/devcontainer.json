{
  "name": "Umbra Backend",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {}
  },
  "service": "app",
  "forwardPorts": [8000, 5432],
  "portsAttributes": {
    "8000": { "label": "FastAPI", "onAutoForward": "notify" },
    "5432": { "label": "PostgreSQL", "onAutoForward": "silent" }
  },
  "postCreateCommand": "bash .devcontainer/setup.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylint",
        "sumneko.lua"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.terminal.activateEnvironment": true
      }
    }
  },
  "secrets": {
    "WCL_CLIENT_ID": {
      "description": "Warcraft Logs API client ID (from https://www.warcraftlogs.com/api/clients)"
    },
    "WCL_CLIENT_SECRET": {
      "description": "Warcraft Logs API client secret"
    }
  },
  "containerEnv": {
    "DATABASE_URL": "postgresql+psycopg://postgres:umbra@localhost:5432/umbra",
    "WCL_CLIENT_ID": "${localEnv:WCL_CLIENT_ID}",
    "WCL_CLIENT_SECRET": "${localEnv:WCL_CLIENT_SECRET}"
  }
}
